// ไฟล์ตั้งค่าต่างๆ (Configuration)

const CONFIG = {
    // หากต้องการให้ข้อมูลเด้งเข้า Google Sheets สามารถนำ Webhook URL มาใส่ตรงนี้ในอนาคต
    // WEBHOOK_URL: "https://script.google.com/macros/s/YOUR_WEBHOOK_URL/exec",
    
    APP_NAME: "Prompt Post Survey",
    VERSION: "2.0"
};

/* 
  หมายเหตุ: 
  ลบฟังก์ชันสุ่มข้อมูล (Mock Data 50 ชุด) และโค้ดสำหรับนำเสนอออกไปทั้งหมดแล้ว
  ตามความต้องการที่จะเก็บเฉพาะ "ข้อมูลจริง (Real Data)" เท่านั้น 
*/
